const fs = require('fs');
let code = fs.readFileSync('src/css/player.css', 'utf8');

// 1. Update .ctrl
const ctrlTarget = `.ctrl {
  -webkit-appearance: none;
  appearance: none;
  background: none;
  border: none;
  color: var(--text-secondary);
  cursor: pointer;
  width: 44px;
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  transition: color .15s, background-color .15s, tra  transition: color .15s, background-color .15s, tra  transition: color .15s, background-color .15s, tra  transition: color .15s, background-color .15s, tra  transition: color .15s, background-color .15s, tra  transition: color .15s, background-color .15s, tra  transition: color .15s, background-color .15s, tra  transition: color .15s, background-color .15s, tra  transition: color .15s, background-color .15s, tra  transition: color .15s, background-color .15s, tra  transition: color .15s, background-color .15s, tra  transition: color .15s, background-color .ctr  transition: color .15s, bacer   transition: color .15s, background-color .15s, tra  transition: coar(--text);\n  backgro  transition: color .15s, `,  transition: color .15s,Syn  transition: coloer  transition: coloOF  transition: er.cjs
rm fix_player.cjs
